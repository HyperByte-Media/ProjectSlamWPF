<Window x:Class="ProjectSlam.App.Features.Core.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjectSlam.App.Features.Core"
        mc:Ignorable="d"
        Title="Project Slam" Height="1080" Width="1920" FontSize="14" 
        WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="10">
            <Button Content="Data Manager" Margin="0 5"/>
            <Button Content="Settings" Margin="0 5"/>
        </StackPanel>

        <!-- Content Area -->
        <ContentPresenter Grid.Column="1" Content="{Binding CurrentView}" />
    </Grid>
</Window>
